/**
 * The type line is printed directly below the illustration. It contains the
 * card’s card type(s). It also contains the card’s subtype(s) and supertype(s),
 * if applicable
 */
package tegenton.card.game.type;

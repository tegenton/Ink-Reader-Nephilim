/**
 * A card can have one or more subtypes printed on its type line. If a card with
 * multiple card types has one or more subtypes, each subtype is correlated to
 * its appropriate card type.
 */
package tegenton.card.game.type.subtype;
